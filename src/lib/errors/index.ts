// import { ErrorType } from "@/types/errors";

// export const extractFirstErrorMessage = (error: ErrorType): string => {
//   const key = Object.keys(error.data.errors)[0];
//   return error.data.errors[key]?.[0] || "حدث خطأ غير متوقع";
// };
